

const geocode = (address, callback) => {
    const obj = {
        latitude: '123',
        longitude: '456',
        location: 'BR'
    }

    const error = null;

    callback(error, obj);
}


export default geocode;